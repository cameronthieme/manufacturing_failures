from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='trying to predict manufacturing failures (old Kaggle)',
    author='Cameron',
    license='MIT',
)
